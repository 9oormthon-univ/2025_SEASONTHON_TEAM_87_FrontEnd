name: bluffing_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 로그인 등 API 통신을 위해 필요합니다.
  http: ^1.5.0

  # STOMP 통신을 위해 필요합니다.
  stomp_dart_client: ^1.0.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # assets 폴더를 등록하는 부분
  assets:
    - assets/